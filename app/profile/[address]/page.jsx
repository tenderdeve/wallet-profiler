// TODO Day 19 (placeholder) → Day 20–22 (full implementation)
// Assembles: WalletProfile + ActivityFeed + StatsDashboard

export default function ProfilePage({ params }) {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <p className="text-gray-400">Profile for {params.address} — coming Day 20</p>
    </main>
  );
}
